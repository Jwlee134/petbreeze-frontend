import React from "react";
import { ScrollView } from "react-native";
import styled from "styled-components/native";

const UpdateProfile = () => {
  return <ScrollView contentContainerStyle={{}}></ScrollView>;
};

export default UpdateProfile;
